#!/usr/bin/env python3
"""Tuples and Sequences"""

# A special fix is used for tuples containing 0 or 1 item
empty = ()
single = 'first',
print(len(empty))
print(len(single))

print()

# Sequence unpacking works long as there are as many variables on the left that 
# equals elements in the sequence
t = 'hello', 6, 3456
x, y, z = t
print(x)
print(y)
print(z)